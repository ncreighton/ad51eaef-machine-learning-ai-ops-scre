# ad51eaef-machine-learning-ai-ops-scre
Tired of manually pasting screenshots into separate annotation tools every time you spot a model drift or need to label edge cases?  This Chrome extension turns your browser into an instant data labeling and AI ops command center. Capture any screen region, add bounding boxes, polygons, or keypoints, and export directly to COCO JSON—all without lea
